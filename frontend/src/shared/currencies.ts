/**
 * Mirrors SupportedCurrencies on the backend, which validates against the same set.
 * UAH is the anchor the exchange-rate table converts through.
 */
export const CURRENCIES = ["UAH", "USD", "EUR"];
